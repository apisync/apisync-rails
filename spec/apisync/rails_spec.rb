require "spec_helper"

RSpec.describe Apisync::Rails do
  it "has a version number" do
    expect(Apisync::Rails::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
